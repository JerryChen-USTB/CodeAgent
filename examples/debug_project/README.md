# Debug Example Project

This tiny project is used by `examples/task.yaml` to exercise the non-interactive
CLI run path without consuming LLM tokens. The example supplies a static pytest
failure log, so the debugging stage can produce reports without executing tests
or modifying project files.

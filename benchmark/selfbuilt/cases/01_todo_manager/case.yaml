schema_version: 1
case_id: 01_todo_manager
title: Todo Manager
difficulty: introductory
task_type: project_implementation_testing_debug_repair
language: python
project_type: tui
stages:
  - implementation
  - testing
  - debugging
  - repair
entrypoint:
  command: "python -m todo_manager"
workspace:
  path: workspace
  initial_state: empty
input_materials:
  - path: input/PRD.md
    type: prd
    required: true
  - path: input/user_stories.md
    type: user_stories
    required: true
  - path: input/design_model.md
    type: design_model
    required: true
  - path: input/acceptance_criteria.md
    type: acceptance_criteria
    required: true
agent_visibility:
  visible_paths:
    - input
    - workspace
  hidden_paths:
    - oracle_tests
test_command:
  command: "python -m unittest discover -s oracle_tests"
  timeout_seconds: 15
success_criteria:
  - The agent creates a runnable todo_manager package in the empty workspace.
  - The default entrypoint starts a simple interactive TUI that can be driven through stdin.
  - TUI behavior, JSON persistence, validation, and filtering match the input materials.
  - All hidden oracle tests pass.

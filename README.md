# CodeAgent

CodeAgent is a CLI-based software-engineering agent for the workflow
`implement -> test -> debug -> repair`. The project is being implemented from
the milestone plan in `docs/codex/plans.md`.

## Development

```powershell
python -m pytest -q
python -m codeagent --help
```
